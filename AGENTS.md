# 智能体规则

<!-- context7 -->
当用户询问库、框架、SDK、API、CLI 工具或云服务时，必须用 Context7 MCP 获取当前文档。即使是 React、Next.js、Prisma、Express、Tailwind、Django、Spring Boot 这类常见技术也一样。适用范围包括 API 语法、配置、版本迁移、库相关排错、安装设置和 CLI 用法。即使你认为自己知道答案，也要使用 Context7；训练数据可能已经过期。库文档优先使用 Context7，而不是普通网页搜索。

不要用于：重构、从零写脚本、业务逻辑排错、代码审查或一般编程概念。

## 步骤

1. 除非用户提供 `/org/project` 格式的精确库 ID，否则先用库名和用户问题调用 `resolve-library-id`。
2. 选择最佳匹配：精确名称、描述相关性、代码片段数量、来源声誉和 benchmark 分数。结果不对时，用替代名称或改写问题重试。
3. 用选中的库 ID 和用户完整问题调用 `query-docs`，不要只传单个关键词。
4. 基于取回的文档回答。
<!-- context7 -->

## 仓库定位

这个仓库是独立的量化研究学习与知识系统。它补充 `../vortex_quant`，但不负责生产策略、回测、执行或交易代码。

本仓库用于：

- 阅读和综合论文、书籍、文章、研报和专业笔记。
- 探索研究方向、假设、心智模型、AI 模型训练想法和可行性。
- 保存外部输入的来源、机制、证据边界和开放问题。
- 把可复用流程沉淀为本地技能。
- 维护结构化学习笔记，为后续实现仓库提供输入。

本仓库不用于：

- 实现生产研究 runner、因子库、回测、QMT/MiniQMT 执行或实盘工具。
- 维护已经绑定 Vortex 本地数据、runner、artifact、preset 或交易链路的因子研究结论。
- 维护产品、架构、代码功能、用户手册或运行规则。
- 默认搬运 `vortex_quant` 的内容。
- 把旧结论当作探索问题时的固定教条。

## 与 vortex_quant 的关系

`vortex_quant` 是下游实现与运行仓库。`vortex_docs` 是上游学习、探索和想法成形仓库。

需要时可以借鉴 `vortex_quant` 的结构方式，例如本地技能和知识归档。不要复制它的研究内容，除非用户明确要求比较。

如果某条学习笔记进入实现阶段，默认让 `vortex_quant` 直接读取本仓库的来源笔记或 skill。只有用户明确要求时，才写实现前检查清单；具体因子研究、策略结论、产品规则和代码绑定投资框架必须沉淀在 `vortex_quant/docs`。

## Obsidian 知识库规则

本仓库也是 Obsidian 知识库。持久 Markdown 笔记必须遵循 `.codex/skills/obsidian`：

- 使用包含 `tags`、`aliases`、`created`、`updated` 的 YAML frontmatter。
- 用 `[[wikilink]]` 表达稳定知识关系。
- 新建笔记时维护相关内容地图页面。
- 原始 PDF 和大文件保存在 `library/`；整理后的知识放在 `knowledge/`。
- 批量编辑后运行本地 wikilink 校验脚本。

## 默认产物

对于新文章、论文或书籍章节，优先产出以下一种或多种：

- 来源卡：书目信息、路径或链接、资料类型、为什么重要。
- 阅读笔记：问题、机制、证据、假设、边界和可迁移点。
- 可行性备忘录：数据要求、评估设计、失败模式和实现边界。
- 技能候选：当一个流程值得反复使用时，沉淀为 `.codex/skills/<name>/SKILL.md`。
- 实现前检查清单：只有用户明确要求把一个想法交给 `vortex_quant` 时才写；不复制知识正文。

## 来源纪律

对近期论文、库、API、市场实践、产品文档或任何可能变化的信息，回答前要获取当前来源。库、框架和 API 文档遵循上面的 Context7 规则。

总结受版权保护的书籍或文章时，只做归纳；必要引用必须短。
